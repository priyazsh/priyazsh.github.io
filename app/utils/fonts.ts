import { Kode_Mono, Cabin, Epilogue } from 'next/font/google';

export const kodeMono = Kode_Mono({
  weight: ['400', '600'],
  subsets: ['latin'],
});

export const cabin = Cabin({
  weight: ['400', '500', '600', '700'],
  subsets: ['latin'],
});

export const epilogue = Epilogue({
  weight: ['400', '500', '600'],
  subsets: ['latin'],
});